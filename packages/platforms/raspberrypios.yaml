platform: raspberrypios
package_manager: apt
default_profile: minimal

packages:
  base:
    - git
    - git-crypt
    - gnupg
    - syncthing
    - build-essential

platform_specific: []

services:
  - ssh
  - syncthing

performance:
  gpu_mem: 16
  overclock: false
