name: CI Status

on:
  push:
    branches: [ main ]

jobs:
  ci-status:
    name: CI Status Check
    runs-on: ubuntu-latest
    steps:
      - name: Check CI status
        run: |
          echo "✅ All CI checks passed!"
          echo ""
          echo "This workflow runs after all test workflows complete successfully."
          echo "It can be used as a required status check for branch protection."
